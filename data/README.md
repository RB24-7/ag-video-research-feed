# Generated Data

`video-manifest.json` is generated on the server by:

```bash
node scripts/build-dropbox-manifest.mjs
```

Do not hand-edit or commit `video-manifest.json`. Update the Dropbox folders, then rerun the builder.
